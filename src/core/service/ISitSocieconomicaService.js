export default class ISitSocieconomicaService {
    constructor () {
        if (!this.createSitSocieconomica) throw new Error(`Method createSitSocieconomica not implemented in ${this.constructor.name})`);
    }
}